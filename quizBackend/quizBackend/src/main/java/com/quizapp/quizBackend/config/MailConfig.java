package com.quizapp.quizBackend.config;

import org.springframework.context.annotation.Configuration;

// JavaMailSender is auto-configured by Spring Boot via application.properties.
// Add any custom beans here if needed (e.g. MimeMessageHelper factory).
@Configuration
public class MailConfig {
    // intentionally empty — auto-config handles everything
}
